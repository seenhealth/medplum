# resource-timeline

Upstream: `packages/react/src/ResourceTimeline/ResourceTimeline.tsx`. Logic is unchanged; presentation and the per-item menu slot follow `timeline.md`.

| Upstream                                                                           | Port                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `getMenu?: (context) => ReactNode`                                                 | Unchanged render-prop. It now returns the **menu contents** for one item (`DropdownMenuItem` / `DropdownMenuLabel` / `DropdownMenuSeparator`), not a Mantine `<Menu.Dropdown>`. `ResourceTimeline` wraps that node in `<TimelineItemMenu>`. |
| `popupMenuItems` forwarded onto each `TimelineItem`                                | Composition: `{menu ? <TimelineItemMenu>{menu}</TimelineItemMenu> : undefined}` as a child of `TimelineItem`.                                                                                                                               |
| `showNotification` / `updateNotification` with a fixed `id: 'upload-notification'` | `notify.show({ loading: true, … })` captures the id; `notify.update(id, …)` on progress / success / error. Same titles and messages. `color: 'teal'` → `'green'`. Mantine `icon` / `withCloseButton` are not part of `notify`.              |
| `<TextInput>` comment box                                                          | `<Input name="text" placeholder="Add comment">` (same `ref` / clear-on-submit).                                                                                                                                                             |
| `<ActionIcon type="submit">` / upload `ActionIcon`                                 | `Button type="submit" size="icon"` / `Button type="button" size="icon"` (`rounded-full`).                                                                                                                                                   |
| `<Loader>` / `<Center>`                                                            | `Spinner` in a flex centering `div`.                                                                                                                                                                                                        |
| `<ScrollArea>` on `AuditEvent`                                                     | `@/components/ui/scroll-area`.                                                                                                                                                                                                              |
| CSS module `.pinnedComment` (`--mantine-color-blue-0`)                             | `bg-blue-50 dark:bg-blue-950` on non-routine `Communication` items.                                                                                                                                                                         |

`loadTimelineResources` still returns `Promise<PromiseSettledResult<Bundle>[]>` and rejected entries are skipped. `sortByDateAndPriority` is `@/lib/medplum/date`. `countToShow` still starts at 10 and "Show More" adds 10.
