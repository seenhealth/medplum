# timing-input

Behavior props (`path`, `name`, `defaultValue`, `onChange`, `disabled`, `defaultModalOpen`) and `data-testid="timinginput-display"` are unchanged. The editor chrome is this package's composition.

| Upstream                                                                                       | Port                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Mantine `Modal` (`opened`, `onClose`, `title`, `closeButtonProps={{ 'aria-label': 'Close' }}`) | `Modal open onOpenChange` + `ModalHeader` / `ModalTitle` / `ModalBody` / `ModalFooter`. Dialog close already names "Close"; an extra `aria-label="Close"` control is rendered so `getByLabelText('Close')` still matches |
| `Switch` + `label="Repeat"`                                                                    | `@/components/ui/switch` + `Label htmlFor`                                                                                                                                                                               |
| `Chip.Group` / `Chip` (day-of-week)                                                            | `Toggle` from `@/components/ui/toggle` with the same letter labels, `value`, `role="checkbox"`, and `aria-checked`                                                                                                       |
| `TextInput` / `NativeSelect`                                                                   | `Input` / `NativeSelect` + `NativeSelectOption`                                                                                                                                                                          |
| `FormSection title htmlFor`                                                                    | `FormSection` + `FormSectionLabel` + `FormSectionError`                                                                                                                                                                  |
| `ArrayAddButton` / `ArrayRemoveButton`                                                         | local `Button variant="ghost"` with the same `"Add Time of Day"` text and `data-testid="timing-repeat-timeOfDay-remove-*"`                                                                                               |
